package com;

/**
 *
 Write a function which returns if password is valid
 and write tests to cover cases based on function reqs:
 More than 3 characters but less than 10.
 Must contain letters and numbers.
 There needs to be at least 1 uppercase letter.
 **/

interface Validate {

     boolean isPasswordValid(String password);
}
